# Licensing test fixtures

Synthetic RS256 tokens signed with an ephemeral test key; the private key was discarded. No production credentials or customer data. The public key only validates these fixtures. Product `rcl-test-only`, device `rcl-test-device`, timestamps January 2020. Valid offline token intentionally has no expiry; expired and stale tokens exercise rejection. None can activate Robin Control Lite.
